<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Update</title>
  </head>
  <body class="bg-gray-800 w-full flex flex-col items-center">
    <div class="bg-green-400 p-2 text-white text-2xl w-full font-bold">
      Change Appointment
    </div>
    <form
      action="/update/<%= appointment.id %>"
      method="post"
      class="w-2/6 min-h-min text-white my-4 relative"
    >

      <div class="flex flex-col py-2">
        <label for="title">Title</label>
        <input
          type="text"
          name="title"
          id="title"
          value="<%= appointment.title %>"
          class="rounded w-full p-2 border-2 border-green-300 bg-gray-700"
        />
      </div>
      <div class="flex flex-col py-2">
        <label for="description">Description</label>
        <textarea
          name="description"
          id="description"
          cols="30"
          rows="5"
          class="rounded w-full p-2 border-2 border-green-300 bg-gray-700"
        ><%= appointment.description %>
    </textarea
        >
      </div>
      <button
        type="submit"
        class="rounded-md bg-green-500 hover:bg-green-400 px-3 py-2 text-xl my-2 absolute right-0"
      >
        Update
      </button>
    </form>
  </body>
</html>
